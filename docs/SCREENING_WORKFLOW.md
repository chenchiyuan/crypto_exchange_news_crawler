# 做空网格标的筛选系统 - 推荐运行流程

## 📊 指标与K线周期设计

根据 `specs/001-short-grid-screening/spec.md` FR-010.1 设计文档：

| 指标 | K线周期 | 数量要求 | 说明 |
|------|---------|----------|------|
| **VDR** (波动率-位移比) | **1m** | **1440根** | 24小时完整数据，计算"震荡纯净度" |
| NATR (归一化ATR) | 4h | 300根 | 基础波动率指标 |
| KER (考夫曼效率比) | 4h | 300根 | 趋势vs震荡判断 |
| Hurst指数 | 4h | 300根 | 均值回归特征 |
| 线性回归 (斜率/R²) | 4h | 300根 | 趋势强度判断 |
| Z-Score | 4h | 300根 | 价格偏离度 |
| OVR (持仓/成交比) | - | - | 实时Ticker数据 |
| 资金费率 | - | 最近7天 | 历史资金费率 |
| CVD (累计成交量差) | - | - | 买卖压力背离 |
| 网格参数 | 1h, 1d | 200/50根 | 辅助计算上下限 |

## ✅ 完整执行流程

### 步骤1: 更新4小时K线（主要指标）

```bash
python manage.py update_market_data \
    --warmup-klines \
    --interval 4h \
    --limit 300 \
    --min-volume 50000000
```

**说明**:
- 300根4h K线 = 50天历史数据
- 用于计算NATR/KER/Hurst/线性回归/Z-Score
- 最小成交量5000万USDT过滤流动性不足标的

### 步骤2: 更新1分钟K线（核心指标VDR）⭐

```bash
python manage.py update_market_data \
    --warmup-klines \
    --interval 1m \
    --limit 1440 \
    --min-volume 50000000
```

**说明**:
- **1440根1m K线 = 24小时完整数据**
- VDR (波动率-位移比) 核心指标，识别"原地空转"震荡标的
- 公式: VDR = (累积日内波动率 CIV) / (净位移 Displacement)
- **VDR > 5.0 表示价格震荡纯净，适合网格**

### 步骤3: 更新1小时和日线K线（网格参数）

```bash
# 1小时K线 (网格参数计算)
python manage.py update_market_data \
    --warmup-klines \
    --interval 1h \
    --limit 200 \
    --min-volume 50000000

# 日线K线 (网格参数计算)
python manage.py update_market_data \
    --warmup-klines \
    --interval 1d \
    --limit 50 \
    --min-volume 50000000
```

**说明**:
- 用于计算网格上下限和止盈止损参数
- 200根1h = 8.3天，50根1d = 50天

### 步骤4: 验证缓存状态

```bash
python manage.py cache_stats
```

**预期输出**:
```
======================================================================
K线缓存统计
======================================================================
已缓存标的数: 61 个
各周期缓存情况:
  1m:  61 个标的, 总计 87,840 根K线   ← 1440 × 61
  4h:  61 个标的, 总计 18,300 根K线   ← 300 × 61
  1h:  61 个标的, 总计 12,200 根K线   ← 200 × 61
  1d:  61 个标的, 总计 3,050 根K线    ← 50 × 61
======================================================================
```

### 步骤5: 执行筛选（推荐参数）

#### 基础筛选（Top 20）

```bash
python manage.py screen_simple \
    --min-volume 100000000 \
    --top-n 20
```

#### 严格筛选（应用过滤条件）

```bash
python manage.py screen_simple \
    --min-volume 200000000 \
    --min-vdr 10 \
    --max-ker 0.3 \
    --min-amplitude 300 \
    --min-funding-rate 50 \
    --top-n 10
```

**参数说明**:
- `--min-volume`: 最小24h成交量（USDT），推荐1亿-2亿
- `--min-vdr`: VDR最小值，>10表示"完美震荡"
- `--max-ker`: KER最大值，<0.3表示低效率震荡
- `--min-amplitude`: 最小振幅累计（基点），>300表示高波动
- `--min-funding-rate`: 最小年化资金费率（基点），>50表示多头拥挤
- `--top-n`: 输出Top N标的，推荐10-30

#### 自定义权重筛选

```bash
python manage.py screen_simple \
    --min-volume 100000000 \
    --vdr-weight 0.5 \
    --ker-weight 0.3 \
    --ovr-weight 0.15 \
    --cvd-weight 0.05 \
    --top-n 15
```

**权重说明**:
- 默认权重: VDR(40%) + KER(30%) + OVR(20%) + CVD(10%)
- VDR权重加大 → 更偏向震荡纯净标的
- KER权重加大 → 更偏向非趋势标的

### 步骤6: 查看结果

#### Web动态查询（推荐）

```bash
python manage.py runserver
# 访问: http://127.0.0.1:8000/screening/
```

**功能**:
- 🔍 Token模糊搜索（实时输入，如"BTC"匹配所有含BTC的标的）
- 📊 动态排序（点击表头）
- 🔢 实时筛选（VDR/KER/振幅/资费条件）
- 📅 历史记录查询（下拉选择）
- 📈 可视化指标展示（排名徽章、分数丸、进度条）

#### HTML静态报告

生成的报告文件：`screening_reports/simple_screening_report_YYYYMMDD_HHMMSS.html`

直接用浏览器打开查看。

## 🎯 筛选结果解读

### 关键指标含义

| 指标 | 理想值 | 含义 | 风险提示 |
|------|--------|------|----------|
| **VDR** | > 10 | 震荡纯净度，>10表示"完美震荡" | <5可能处于单边趋势 |
| **KER** | < 0.3 | 价格运动效率，越低越震荡 | >0.5表示趋势性强 |
| **OVR** | 0.5-1.5 | 持仓/成交比，健康杠杆范围 | >2.0高杠杆拥挤风险 |
| **CVD** | 有背离 | 买卖压力背离，做空优势信号 | - |
| **年化资费** | > 50bp | 多头付费空头，做空收益 | 负值表示逼空风险 |
| **综合指数** | > 0.7 | 加权综合评分 | <0.5不推荐 |

### Top 3 标的示例

```
Top 3 标的:
  1. ICPUSDT         综合指数=0.8234 VDR=12.5 KER=0.245 OVR=1.12 CVD=✓
  2. ZENUSDT         综合指数=0.8156 VDR=11.8 KER=0.267 OVR=0.98 CVD=✓
  3. QNTUSDT         综合指数=0.7891 VDR=10.3 KER=0.289 OVR=1.34 CVD=✗
```

## ⏰ 定时任务配置（可选）

每日自动筛选：

```bash
# 编辑crontab
crontab -e

# 添加定时任务（每天上午10点执行）
0 10 * * * cd /path/to/project && /path/to/python manage.py screen_simple --min-volume 100000000 >> /tmp/screening.log 2>&1
```

## 🔧 故障排查

### 问题1: K线数据不足

**症状**: 日志显示"1分钟K线数据不足240根, VDR降级跳过"

**解决**:
```bash
# 重新缓存1m K线（确保1440根）
python manage.py update_market_data --warmup-klines --interval 1m --limit 1440 --min-volume 50000000
```

### 问题2: 缓存数据过期

**症状**: 筛选结果与市场实际不符

**解决**:
```bash
# 清理缓存（可选）
python manage.py shell
>>> from grid_trading.models import KlineDataCache
>>> KlineDataCache.objects.all().delete()
>>> exit()

# 重新缓存所有K线
python manage.py update_market_data --warmup-klines --interval 4h --limit 300
python manage.py update_market_data --warmup-klines --interval 1m --limit 1440
python manage.py update_market_data --warmup-klines --interval 1h --limit 200
python manage.py update_market_data --warmup-klines --interval 1d --limit 50
```

### 问题3: 筛选结果为空

**可能原因**:
1. `--min-volume` 设置过高（尝试降低到50000000）
2. 过滤条件过严（移除 `--min-vdr` 等参数）
3. 市场整体处于单边趋势（等待市场进入震荡期）

## 📚 相关文档

### 筛选系统
- [筛选系统快速开始](SCREENING_QUICKSTART.md)
- [项目架构文档](PROJECT_ARCHITECTURE.md)
- [完整技术规格](../specs/001-short-grid-screening/spec.md)

### 网格交易算法
- [挂单位置优化算法](./entry_algorithm_final.md) - 做空挂单算法（v3.0最终版）✅ 已实施
- [网格参数计算原理](./grid_parameters_analysis.md) - ATR自适应网格参数详解
- [网格数量优化](./grid_optimization_summary.md) - 100层网格限制优化方案
- [算法演进历史](./archive/entry-optimization-evolution/) - v1.0/v2.0归档记录

---

**最后更新**: 2025-12-04
**版本**: v1.0.0
