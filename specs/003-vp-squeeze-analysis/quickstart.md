# Quickstart: VP-Squeeze算法支撑压力位计算服务

**Created**: 2025-11-24

## 概述

VP-Squeeze是一个技术分析工具，通过以下步骤识别加密货币的支撑位和压力位：

1. **Squeeze检测**: 使用Bollinger Bands和Keltner Channels识别低波动率盘整状态
2. **Volume Profile分析**: 基于成交量分布计算关键价位

## 快速开始

### 1. 安装

```bash
# 确保在项目虚拟环境中
source venv/bin/activate

# 迁移数据库
python manage.py migrate
```

### 2. 基础用法

```bash
# 分析ETH 4小时K线（默认100根）
python manage.py vp_analysis --symbol eth --interval 4h

# 分析BTC 1小时K线，使用200根数据
python manage.py vp_analysis --symbol btc --interval 1h --limit 200

# 输出JSON格式
python manage.py vp_analysis --symbol eth --interval 4h --json

# 显示详细计算过程
python manage.py vp_analysis --symbol eth --interval 4h -v
```

### 3. 批量分析

```bash
# 分析多个币种
python manage.py vp_analysis --symbol eth,btc,sol --interval 4h

# 使用预设组合
python manage.py vp_analysis --group top10 --interval 4h
```

## 命令参数

| 参数 | 说明 | 默认值 |
|-----|------|--------|
| `--symbol` | 交易对（逗号分隔多个） | 必填 |
| `--interval` | 时间周期 | 必填 |
| `--limit` | K线数量 | 100 |
| `--group` | 预设币种组合 (top10) | - |
| `--json` | JSON格式输出 | False |
| `-v, --verbose` | 显示详细日志 | False |
| `--save` | 保存结果到数据库 | False |

### 支持的时间周期

`1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d, 3d, 1w, 1M`

### 支持的币种

**TOP 10**: btc, eth, bnb, sol, xrp, doge, ada, avax, dot, matic

## 输出示例

### 文本格式（默认）

```
═══════════════════════════════════════════════════════════════
VP-Squeeze Analysis: ETHUSDT (4h) | 2025-11-24 16:30 UTC
═══════════════════════════════════════════════════════════════

📊 Squeeze状态: ✓ 有效 (连续5根K线)
   可靠度: 高

───────────────────────────────────────────────────────────────
📍 关键价位
───────────────────────────────────────────────────────────────
   支撑位 (VAL):    $3,120.50
   压力位 (VAH):    $3,280.00
   成交量重心 (VPOC): $3,195.00

───────────────────────────────────────────────────────────────
📈 高量节点 (HVN) - 强支撑/阻力区
───────────────────────────────────────────────────────────────
   • $3,180.00 - $3,210.00

📉 低量节点 (LVN) - 价格快速穿越区
───────────────────────────────────────────────────────────────
   • $3,250.00 - $3,270.00

═══════════════════════════════════════════════════════════════
```

### JSON格式

```json
{
  "symbol": "ETHUSDT",
  "interval": "4h",
  "timestamp": "2025-11-24T16:30:00Z",
  "squeeze": {
    "active": true,
    "consecutive_bars": 5,
    "reliability": "high"
  },
  "levels": {
    "val": 3120.50,
    "vah": 3280.00,
    "vpoc": 3195.00
  },
  "hvn": [{"low": 3180.00, "high": 3210.00, "volume": 12500.5}],
  "lvn": [{"low": 3250.00, "high": 3270.00, "volume": 1200.3}],
  "metadata": {
    "klines_count": 100,
    "price_range": {"min": 3050.00, "max": 3350.00},
    "total_volume": 150000.0
  }
}
```

## 关键概念

### Squeeze（盘整状态）

当Bollinger Bands收缩进入Keltner Channels内部时，表示市场处于低波动率盘整状态：

```
BB_Upper < KC_Upper AND BB_Lower > KC_Lower
```

需连续3根K线满足条件才判定为有效Squeeze。

### Volume Profile关键价位

| 价位 | 说明 |
|-----|------|
| **VAL** (Value Area Low) | 70%成交量区间的下限，强支撑位 |
| **VAH** (Value Area High) | 70%成交量区间的上限，强压力位 |
| **VPOC** (Volume POC) | 成交量最大的价格，价格重心 |
| **HVN** (High Volume Node) | 高成交量区域，价格易停留 |
| **LVN** (Low Volume Node) | 低成交量区域，价格快速穿越 |

### 可靠度

- **高 (high)**: Squeeze有效，支撑压力位可信度高
- **低 (low)**: Squeeze无效（趋势市场），支撑压力位仅供参考

## 常见问题

### Q: 数据不足怎么办？

至少需要30根K线才能计算指标。如遇此错误，增加`--limit`参数或选择更短的时间周期。

### Q: 如何在定时任务中使用？

```bash
# crontab示例：每4小时运行一次
0 */4 * * * cd /path/to/project && python manage.py vp_analysis --group top10 --interval 4h --save --json >> /var/log/vp_analysis.log 2>&1
```

### Q: 如何查询历史分析结果？

```python
from vp_squeeze.models import VPSqueezeResult

# 查询最近的ETH分析
results = VPSqueezeResult.objects.filter(
    symbol='ETHUSDT',
    interval='4h'
).order_by('-analyzed_at')[:10]

# 查询有效Squeeze的结果
squeeze_results = VPSqueezeResult.objects.filter(
    squeeze_active=True,
    reliability='high'
)
```

## 算法参数

| 参数 | 值 | 说明 |
|-----|-----|------|
| BB Period | 20 | Bollinger Bands周期 |
| BB Multiplier | 2.0 | 标准差倍数 |
| KC EMA Period | 20 | Keltner Channels EMA周期 |
| KC ATR Period | 10 | ATR周期 |
| KC Multiplier | 1.5 | ATR倍数 |
| Squeeze Bars | 3 | 连续确认K线数 |
| VP Resolution | 0.1% | 价格桶分辨率 |
| Value Area | 70% | 价值区域覆盖比例 |
| HVN Threshold | 80th percentile | 高量节点阈值 |
| LVN Threshold | 20th percentile | 低量节点阈值 |
